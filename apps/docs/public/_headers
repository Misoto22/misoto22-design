# Next emits the share card as an extensionless file, and Cloudflare Pages
# types a response from its extension — without this it ships as
# application/octet-stream and no scraper renders the preview.
/opengraph-image
  Content-Type: image/png
  Cache-Control: public, max-age=3600
